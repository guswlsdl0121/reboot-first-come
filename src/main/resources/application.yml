spring:
  datasource:
    url: jdbc:mysql://db:3306/firstcome_system
    username: docker
    password: docker1234
    driver-class-name: com.mysql.cj.jdbc.Driver

  data:
    redis:
      host: cache
      port: 6379

  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false